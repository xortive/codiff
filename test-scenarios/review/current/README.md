# Current Review Scenarios

Each scenario here has one current base-to-head review target. The histories are reusable inputs:
the baseline conformance gate treats them as ordinary single-diff reviews, while later review-mode
coverage can add stronger expectations to the scenario definitions.
