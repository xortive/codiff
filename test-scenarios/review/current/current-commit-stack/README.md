# Current Commit Stack

## Purpose

Materialize a coherent layered notification-preferences change as one current review target.

## Target

```text
main -> feature/test-scenario
```

## Proves

- The shared harness creates a deterministic base and ordered feature history.
- Checkpoints expose the base and each immutable feature commit to downstream fixtures.
- The current review can be evaluated without provider or review-history state.
